# shared/

Placeholder directory for shared code between platforms.

This folder was created by the assistant at your request. It currently contains only this README and a `.gitkeep` so Git will track the empty directory.

Next steps you might want:
- Move common or multiplatform source code here (for example a Kotlin Multiplatform module).
- Add a Gradle module (settings.gradle.kts and build scripts) if you want this to be a standalone module.
- If you want, I can scaffold a KMM module here (sources, Gradle entries) — tell me and I'll do it.
