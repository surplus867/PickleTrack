// File removed per user request. Previously contained adapters between shared presentation ViewModels and platform UI interfaces.
// If you want the file deleted from the repository entirely, remove it with git: `git rm` and commit.
